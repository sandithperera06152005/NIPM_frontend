import { Routes } from '@angular/router';
import { InventoryExportComponent } from './inventory-export.component';

const routes: Routes = [
  {
    path: '',
    component: InventoryExportComponent,
  },
];

export default routes;
