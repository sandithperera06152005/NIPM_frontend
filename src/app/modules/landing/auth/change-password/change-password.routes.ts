import { Routes } from '@angular/router';
import { AuthChangePasswordComponent } from 'app/modules/landing/auth/change-password/change-password.component';

export default [
    {
        path: '',
        component: AuthChangePasswordComponent,
    },
] as Routes;
